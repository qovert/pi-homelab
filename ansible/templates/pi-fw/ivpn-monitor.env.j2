# Managed by Ansible — do not edit manually
# Hits pi-01's LAN IP directly, not the public chat.bitrot.me hostname --
# the whole point is this alert path must not depend on DNS, the tunnel,
# or Cloudflare, all of which may be exactly what's broken when it fires.
ROCKETCHAT_WEBHOOK_URL=http://{{ inventory_hosts.pi_01 }}:3000{{ vault.monitoring.rocketchat_webhook_path }}
